using UnityEngine;

namespace SpaceGame
{
	/// <summary>
	/// Read Only attribute.
	/// Attribute is used only to mark ReadOnly properties.
	/// </summary>
	public class ReadOnlyAttribute : PropertyAttribute { }
}
